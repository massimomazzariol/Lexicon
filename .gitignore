.env.local
node_modules/
.dart_tool/
.packages
build/
pubspec.lock
.idea/
.vscode/
coverage/
dist/
.tmp/

# Generated report snapshots in docs/data/
docs/data/*_2[0-9][0-9][0-9]-*.json
docs/data/*_2[0-9][0-9][0-9]-*.csv

# Authoring workspace (seeding + experiments) is LOCAL ONLY - never published. The
# platform ships ready packs (packs/, dist via `release`); seeding can always restart
# from scratch locally. The reusable tooling lives in tools/; this dir is just the
# working data (wishlist, drafts, cache, seed lists).
authoring/

# Internal planning/design docs - kept local, not published.
docs/planning/
